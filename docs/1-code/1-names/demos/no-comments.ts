// * avoid comments by writing self-explanatory code
// Exceptions: public API documentation, warnings, or any temporary or todo comments

const d = 5; // ! days
// ! print the schedule
function print(d: number) {
	// ! for the next d days
	console.log("your schedule for the next", d, "days");
}
